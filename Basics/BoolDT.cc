#include <iostream>

using namespace std; 

int main(){
    //Boolean data types hold binary values (1,0) or True False. 
    //Any value greater than 0 has a boolean true value, whereas less than 0 is False.
    bool isOn = 1;
    cout<<isOn;


    return 0;

}